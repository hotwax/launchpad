import { api, client, hasError, logout, initialise, resetConfig, updateUrls, updateToken } from '@hotwax/oms-api'

export {
  api,
  client,
  hasError,
  logout,
  initialise,
  resetConfig,
  updateUrls,
  updateToken
}